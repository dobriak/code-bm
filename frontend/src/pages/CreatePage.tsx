/**
 * Create page — two-pane playlist builder.
 * Left pane: track browser with search and filters.
 * Right pane: playlist with drag-and-drop reorder.
 */
import PlaylistBuilder from "../components/PlaylistBuilder";

export default function CreatePage() {
  return <PlaylistBuilder />;
}
